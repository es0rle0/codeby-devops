apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: dev
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: local-sc
  volumeName: pv-mysql
  resources:
    requests:
      storage: 1Gi
